syntax = "proto2";
package NRMatrix;

message NR_SRS_PACK{
  repeated NR_SRS_INFO Matrix = 1;
}
message NR_SRS_INFO{
  repeated RESULT PRB_ITEM  = 3;
}
// RESULT存放的其实是和信道相关的复数结构，由于以上指标都是复数形式所以就统一命名为RESULT
message RESULT {
  required int32 image = 1;
  required int32 real  = 2;
}